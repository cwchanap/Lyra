{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the layout editor window",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
